<!DOCTYPE html>
<html lang="en">

<head>
    {{> header}}
    <link rel="stylesheet" type="text/css" href="/css/hashtag-list.css">
    <title> HNNews </title>
</head>

<body>
    {{>nav}}

    <div class="container">
        <div id="scroll-smooth"></div>

        <div class="nav-search">
            <div class="title-nav-search">DANH SÁCH NHÃN DÁN</div>
            <form method="GET" action="/nhandan-timkiem" class="col-md-12" style="padding: 0">
                <input class="input-search" type="text" placeholder="Nhập nhãn dán tìm kiếm..." name="tagsearch"
                    ng-model="searchItem">
            </form>
        </div>
        {{#if none_result}}

        <div style="margin: 0px 130px 110px 130px">
            <div class="row row-margin row-hashtag" style="display: flex; justify-content: center;">
                <p> KHÔNG CÓ KẾT QUẢ TÌM KIẾM </p>
            </div>
            <div class="row row-margin row-hashtag" style="display: flex; justify-content: center;">
                <button onclick="window.location.href = '/nhandan';" type="button" class="btn btn-dark close-btn"> Quay
                    về danh sách nhãn dán
                </button>
            </div>
        </div>

        {{else}}

        <div style="margin: 0px 130px 30px 130px">
            <div class="row row-margin row-hashtag">
                {{#each datasearch}}
                <a href="/nhandan/{{tag_id}}" class="col-md-3 singletag">
                    <div class="row row-margin hashtag-img">
                        <div class="col-md-12">
                            <img class="img" src="/img/hashtag-pic.png">
                        </div>
                    </div>
                    <div class="row row-margin title-img">
                        <div class="col-md-6 tag-name">{{tag_name}}</div>
                        <div class="col-md-6 tag-value">{{tagvalue}} bài</div>
                    </div>
                </a>
                {{/each}}
            </div>
        </div>

        {{/if}}

        {{!-- Pagination --}}
        <!-- <nav class="nav-pagination" aria-label="...">
            <ul class="pagination">
                {{#with btnPrevious}}
                {{#if disable}}
                <li class="page-item disabled">
                    {{else}}
                <li class="page-item">
                    {{/if}}
                    <a class="page-link" href="?page={{value}}" aria-label="Previous">
                        <span aria-hidden="true">&laquo;</span>
                        <span class="sr-only">Previous</span>
                    </a>
                </li>
                {{/with}}

                {{#each tagpagination}}
                {{#if active}}
                <li class="page-item active"><a class="page-link" href="?page={{value}}">{{value}}</a></li>
                {{else}}
                <li class="page-item"><a class="page-link" href="?page={{value}}">{{value}}</a></li>
                {{/if}}
                {{/each}}

                {{#with btnNext}}
                {{#if disable}}
                <li class="page-item disabled">
                    {{else}}
                <li class="page-item">
                    {{/if}}
                    <a class="page-link" href="?page={{value}}" aria-label="Next">
                        <span aria-hidden="true">&raquo;</span>
                        <span class="sr-only">Next</span>
                    </a>
                </li>
                {{/with}}
            </ul>
        </nav> -->
    </div>


    {{>footer}}

    {{!-- Bootstrap 4.3.1 --}}
    <script src="https://code.jquery.com/jquery-3.3.1.slim.min.js"
        integrity="sha384-q8i/X+965DzO0rT7abK41JStQIAqVgRVzpbzo5smXKp4YfRvH+8abtTE1Pi6jizo"
        crossorigin="anonymous"></script>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/popper.js/1.14.7/umd/popper.min.js"
        integrity="sha384-UO2eT0CpHqdSJQ6hJty5KVphtPhzWj9WO1clHTMGa3JDZwrnQq4sF86dIHNDz0W1"
        crossorigin="anonymous"></script>
    <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.3.1/js/bootstrap.min.js"
        integrity="sha384-JjSmVgyd0p3pXB1rRibZUAYoIIy6OrQ6VrjIEaFf/nJGzIxFDsf4x0xIM+B07jRM"
        crossorigin="anonymous"></script>

    {{!-- JQuery 3.3.1 --}}
    <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.3.1/jquery.min.js"></script>

    {{!-- Script --}}
    <script>
        $(document).ready(function () {
            // Auto scroll to top
            $(window).scrollTop(0);
            $(".menu").hide();
            $(".line-under-menu").hide();

            $(window).scroll(function () {
                if ($(window).scrollTop() > 27) {
                    $("#scroll-smooth").css("display", "block");
                    $("#scroll-nav").addClass("position-fixed");
                    $("#scroll-nav").addClass("bootstrap-position-fixed");
                }
                else {
                    $("#scroll-smooth").css("display", "none");
                    $("#scroll-nav").removeClass("position-fixed");
                    $("#scroll-nav").removeClass("bootstrap-position-fixed");
                }
            });
        })
    </script>
</body>

</html>